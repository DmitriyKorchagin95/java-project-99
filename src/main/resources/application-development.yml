spring:
  datasource:
    url: jdbc:h2:mem:devdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  h2:
    console:
      enabled: true
debug: true
logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG
    org.apache.coyote.http11: DEBUG